import React from 'react'
//import { JWT_TOKEN } from '../../shared/constans';
import HomeTeacher from '../inscriptionTeacher/HomeTeacher';

const Home = () => {

    //const JWT= localStorage.getItem(JWT_TOKEN);

  return (
    <HomeTeacher/>
  )
}

export default Home